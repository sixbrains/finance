import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.tuempresa.gastos',
  appName: 'Gastos App',
  webDir: 'dist',
  bundledWebRuntime: false
};
export default config;
